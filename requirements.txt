# Core agent framework
langgraph>=0.2.0
langchain>=0.2.0
langchain-community>=0.2.0

# Local LLM via Ollama
ollama>=0.2.0

# Kubernetes client
kubernetes>=29.0.0

# Observability / metrics
prometheus-api-client>=0.5.4

# Database
# sqlite3 is part of the Python standard library — no extra package needed

# Terminal UI
rich>=13.7.0

# Configuration & data
pyyaml>=6.0.1
python-dotenv>=1.0.0
pydantic>=2.7.0

# Embeddings / RAG (lightweight, local)
sentence-transformers>=3.0.0
faiss-cpu>=1.8.0

# Scheduling (Janitor cron)
apscheduler>=3.10.4

# Utilities
httpx>=0.27.0
tenacity>=8.3.0
